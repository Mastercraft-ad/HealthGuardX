# Copy this file to `.env` and fill in real credentials.
# DO NOT commit .env with secrets to version control.

# Example Postgres connection string (replace values):
# postgres://username:password@localhost:5432/healthguardx
# or for Neon/other providers use their provided connection string.
DATABASE_URL=postgresql://user:password@localhost:5432/healthguardx

# Optional - port the server should listen on (defaults to 5000)
PORT=5000
